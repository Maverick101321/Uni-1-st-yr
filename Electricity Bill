/* The electricity officer has mentioned the total counts of unit and amount. The officer informs the customer the bill amount in a uniques format. 
The format given by electricity as follows: Total Bill = unitconsumed^costperunit. */

#include <stdio.h>
#include <math.h>

int main()
{
    int unitconsumed,totalbill;
    int costperunit;
    scanf("%d",&unitconsumed);
    scanf("%d",&costperunit);
    totalbill=pow(unitconsumed,costperunit);
    printf("\n%d",totalbill);
    printf("Hello World");

    return 0;
}
